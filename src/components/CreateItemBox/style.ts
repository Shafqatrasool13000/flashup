import styled from "styled-components";

export const CreateItemStyle = styled.div`
  .left-img {
    cursor: pointer;
  }
  .card {
    .icon {
      cursor: pointer;
    }
  }

  .method-name {
    background-color: red;
    color: white;
    border-radius: 7px;
    outline: none;
    padding: 8px;
    font-size: 14px;
  }
`;
export default CreateItemStyle;
